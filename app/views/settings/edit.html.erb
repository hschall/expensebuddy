<div class="container mt-4" style="max-width: 720px;">
  <h3 class="text-center mb-4">Configuración del Sistema</h3>

  <!-- 🛠️ Ciclo de Tarjeta -->
  <%= form_with(model: @setting, url: settings_path, method: :patch, local: true) do |form| %>
    <div class="mb-3">
      <%= form.label :cycle_end_day, "Día de corte de tarjeta", class: "form-label fw-bold" %>
      <%= form.number_field :cycle_end_day, in: 1..31, class: "form-control", required: true %>
      <div class="form-text">Ejemplo: Si tu fecha de corte es el día 6, escribe <strong>6</strong>.</div>
    </div>

    <div class="mt-3">
      <%= form.submit "Guardar Configuración", class: "btn-custom-primary-sm" %>
    </div>
  <% end %>

  <!-- 🧹 Opciones de limpieza -->
  <hr class="my-4">

  <div class="d-flex flex-column gap-2">
    <%#= button_to "Eliminar todas las transacciones",
      delete_all_transactions_path,
      method: :delete,
      class: "btn-custom-danger-sm",
      onclick: "return confirm('¿Seguro que desea eliminar todos los registros?')" %>

    <%#= button_to "Eliminar todos los pagos de saldo",
      delete_all_balance_payments_path,
      method: :delete,
      data: { turbo: false, confirm: "¿Seguro que desea eliminar todos los pagos de saldo?" },
      class: "btn-custom-danger-sm" %>

    <%= button_to "Eliminar todos los registros (Transacciones y Pagos de Saldo)",
      delete_all_records_path,
      method: :delete,
      class: "btn-custom-danger-sm",
      onclick: "return confirm('¿Estás seguro que deseas eliminar TODAS las transacciones y pagos de saldo?')" %>
  </div>
</div>
